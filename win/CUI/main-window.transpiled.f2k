$if(%isplaying%,$if($if($if(%title%,$trim(%title%),),Title: $if(%title%,$trim(%title%),),),$if($if(%title%,$trim(%title%),),Title: $if(%title%,$trim(%title%),),)$if($if($trim(%track number%),Track: $trim(%track number%),),  '┃'  $if($trim(%track number%),Track: $trim(%track number%),)$if($if(%totaltracks%,$num($trim(%totaltracks%),0),), of $if(%totaltracks%,$num($trim(%totaltracks%),0),),),)$if($if($if(%playback_time%,%playback_time%,),Played: $if(%playback_time%,%playback_time%,),),  '┃'  $if($if(%playback_time%,%playback_time%,),Played: $if(%playback_time%,%playback_time%,),)$if($if(%length%,%length%,), of $if(%length%,%length%,),),)$if($if(%album artist%,$stripprefix($trim(%album artist%),The)),  '┃'  Artist: $if(%album artist%,$stripprefix($trim(%album artist%),The)),)$if($if(%album%,$trim(%album%),),  '┃'  Album: $if(%album%,$trim(%album%),),)$if($if(%date%,%date%,),  '┃'  Date: $year($if(%date%,%date%,)),)    '◈'    ,))%_foobar2000_version%